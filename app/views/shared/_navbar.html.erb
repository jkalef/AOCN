    <nav class="navbar navbar-default my-navbar">
      <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <%# image_tag "toms_logo_trans.png", class: "brand-image" %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
            <li><%= link_to "PLAY NOW", play_path %></li>
              <% if current_user.is_admin? %>
                <li><%= link_to "Stats", admin_stats_path %></li>
                <li><%= link_to "Create New Category", new_admin_category_path %></li>
                <li><%= link_to "Add Items To A Category", admin_categories_path %></li>
              <% end %>
            <% end %>
            <%# modal pop-up for contact us %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to "Edit Profile", edit_profile_path(current_user.profile) %></li>
              <li><%= link_to "Sign Out", sessions_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Sign In", new_session_path %></li>
              <li><%= link_to "Sign Up", new_user_path %></li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <div id="popup">
      <ul>
        <li><%= link_to "Stats Page", admin_stats_path %></li>
        <li><%= link_to "Create New Category", new_admin_category_path %></li>
        <li><%= link_to "Add Items To Category", admin_categories_path %></li>
      </ul>
    </div>

